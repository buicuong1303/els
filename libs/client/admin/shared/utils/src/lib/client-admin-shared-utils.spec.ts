import { clientAdminSharedUtils } from './client-admin-shared-utils';

describe('clientAdminSharedUtils', () => {
  it('should work', () => {
    expect(clientAdminSharedUtils()).toEqual('client-admin-shared-utils');
  });
});
