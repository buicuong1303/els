import { serverDictionaryContainDataAccessType } from './server-dictionary-contain-data-access-type';

describe('serverDictionaryContainDataAccessType', () => {
  it('should work', () => {
    expect(serverDictionaryContainDataAccessType()).toEqual(
      'server-dictionary-contain-data-access-type'
    );
  });
});
