import { serverDictionaryPhraseTranslationDataAccessType } from './server-dictionary-phrase-translation-data-access-type';

describe('serverDictionaryPhraseTranslationDataAccessType', () => {
  it('should work', () => {
    expect(serverDictionaryPhraseTranslationDataAccessType()).toEqual(
      'server-dictionary-phrase-translation-data-access-type'
    );
  });
});
