import { serverDictionaryDetailDataAccessServices } from './server-dictionary-detail-data-access-services';

describe('serverDictionaryDetailDataAccessServices', () => {
  it('should work', () => {
    expect(serverDictionaryDetailDataAccessServices()).toEqual(
      'server-dictionary-detail-data-access-services'
    );
  });
});
