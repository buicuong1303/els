import { serverDictionaryJoinDataAccessTypes } from './server-dictionary-join-data-access-types';

describe('serverDictionaryJoinDataAccessTypes', () => {
  it('should work', () => {
    expect(serverDictionaryJoinDataAccessTypes()).toEqual(
      'server-dictionary-join-data-access-types'
    );
  });
});
