import { serverLearningAvailableMissionDataAccessTypes } from './server-learning-available-mission-data-access-types';

describe('serverLearningAvailableMissionDataAccessTypes', () => {
  it('should work', () => {
    expect(serverLearningAvailableMissionDataAccessTypes()).toEqual(
      'server-learning-available-mission-data-access-types'
    );
  });
});
