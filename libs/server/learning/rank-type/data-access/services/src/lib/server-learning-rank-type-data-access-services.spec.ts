import { serverLearningRankTypeDataAccessServices } from './server-learning-rank-type-data-access-services';

describe('serverLearningRankTypeDataAccessServices', () => {
  it('should work', () => {
    expect(serverLearningRankTypeDataAccessServices()).toEqual(
      'server-learning-rank-type-data-access-services'
    );
  });
});
