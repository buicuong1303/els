import { serverLearningRankTypeDataAccessTypes } from './server-learning-rank-type-data-access-types';

describe('serverLearningRankTypeDataAccessTypes', () => {
  it('should work', () => {
    expect(serverLearningRankTypeDataAccessTypes()).toEqual(
      'server-learning-rank-type-data-access-types'
    );
  });
});
