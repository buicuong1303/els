import { serverLearningRankLogDataAccessServices } from './server-learning-rank-log-data-access-services';

describe('serverLearningRankLogDataAccessServices', () => {
  it('should work', () => {
    expect(serverLearningRankLogDataAccessServices()).toEqual(
      'server-learning-rank-log-data-access-services'
    );
  });
});
