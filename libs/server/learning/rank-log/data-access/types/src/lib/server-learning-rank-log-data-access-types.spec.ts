import { serverLearningRankLogDataAccessTypes } from './server-learning-rank-log-data-access-types';

describe('serverLearningRankLogDataAccessTypes', () => {
  it('should work', () => {
    expect(serverLearningRankLogDataAccessTypes()).toEqual(
      'server-learning-rank-log-data-access-types'
    );
  });
});
