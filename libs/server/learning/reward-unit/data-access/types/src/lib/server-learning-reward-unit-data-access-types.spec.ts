import { serverLearningRewardUnitDataAccessTypes } from './server-learning-reward-unit-data-access-types';

describe('serverLearningRewardUnitDataAccessTypes', () => {
  it('should work', () => {
    expect(serverLearningRewardUnitDataAccessTypes()).toEqual(
      'server-learning-reward-unit-data-access-types'
    );
  });
});
